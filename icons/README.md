# Icons

这里需要放置 3 个 PNG 图标文件：

- `icon16.png` (16x16) - 工具栏 / 菜单小图标
- `icon48.png` (48x48) - 扩展管理页
- `icon128.png` (128x128) - Chrome 商店 / 安装页

如果暂时没有图标，可以：
1. 用任意图片工具生成蓝色圆形 + "333" 文字的 PNG；
2. 或者暂时删除 `manifest.json` 中的 `icons` 字段，扩展仍可正常加载。
